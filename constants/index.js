import assets from "./assets";
import { COLORS, SHADOWS, SIZES, FONTS } from "./theme";
import { NFTData, servicesData, reviewsData, stories, popular, categories } from "./dummy";

export { assets, COLORS, SHADOWS, SIZES, FONTS, NFTData, servicesData, reviewsData, stories, popular, categories};
